{{! views/partials/header.hbs }}
<header class="header">
  <div class="nav-overlay" id="navOverlay"></div>
  <div class="container">
    <div class="header-content">
      <div class="logo">
        <a href="/">MED_EDU</a>
      </div>
      <button class="burger-menu" id="burgerMenu">
        <span></span>
        <span></span>
        <span></span>
      </button>
      <nav class="nav" id="mobileNav">
        <a href="/">Головна</a>
        <a href="/courses">Навчальні курси</a>
        <a href="/certification">Сертифікація</a>
        <a href="/about">Про нас</a>
        {{!-- {{#unless user}}
        <div class="nav-divider"></div>
        <a href="#" class="nav-action" data-toggle="modal" data-target="#loginModal">🔑 Вхід</a>
        <a href="#" class="nav-action primary" data-toggle="modal" data-target="#registerModal">🚀 Записатися</a>
        {{/unless}}
        {{#if user}}
        <div class="nav-divider"></div>
        <a href="/profile" class="nav-action">👤 Профіль</a>
        <a href="/logout" class="nav-action logout">🚪 Вийти</a>
        {{/if}} --}}
        <div class="header-actions">
          {{#if user}}
            <div class="dropdown d-inline-block">
              <a
                href="#"
                class="btn-secondary dropdown-toggle d-flex align-items-center"
                id="profileDropdown"
                data-toggle="dropdown"
                aria-haspopup="true"
                aria-expanded="false"
                style="padding: 0.25rem 0.75rem;"
              >
                <img
                  src="{{user.photo_url}}"
                  alt="avatar"
                  class="rounded-circle"
                  width="32"
                  height="32"
                  style="object-fit:cover; margin-right:8px;"
                />
                {{user.full_name}}
              </a>
              <div
                class="dropdown-menu dropdown-menu-right"
                aria-labelledby="profileDropdown"
              >
                <a class="dropdown-item" href="/profile">Профіль</a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="/logout">Вийти</a>
              </div>
            </div>
          {{else}}
            <a
              href="#"
              class="btn-primary"
              data-toggle="modal"
              data-target="#loginModal"
            >Вхід</a>
          {{/if}}
        </div>
      </nav>

    </div>
  </div>
</header>